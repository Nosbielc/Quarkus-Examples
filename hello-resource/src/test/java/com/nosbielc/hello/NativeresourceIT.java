package com.nosbielc.hello;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeresourceIT extends HelloResourceTest {

    // Execute the same tests but in native mode.
}